{
  "name": "planning-olda",
  "version": "1.0.0",
  "description": "Planning OLDA",
  "type": "commonjs",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "test": "for f in test/*.test.js; do node \"$f\" || exit 1; done"
  },
  "engines": {
    "node": ">=18"
  },
  "dependencies": {
    "express": "^4.19.2",
    "pg": "^8.12.0"
  },
  "devDependencies": {
    "pg-mem": "^3.0.5"
  }
}
